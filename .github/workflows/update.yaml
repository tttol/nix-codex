name: update-codex-version
on:
  push:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  update-sources:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: cachix/install-nix-action@526ededb8b015c91fd6c3ce4c20df16251eab003 # v32
        with:
          github_access_token: ${{ github.token }}
      - id: update
        run: |
          VERSION=$(./update.sh | tail -1)
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
        env:
          GITHUB_TOKEN: ${{ github.token }}
      - run: git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
      - run: git config user.name "github-actions[bot]"
      - run: git add versions.json
      - run: |
          if ! git diff --staged --quiet; then
            git commit -m "chore: update codex to ${{ steps.update.outputs.version }}"
            git tag "${{ steps.update.outputs.version }}"
            git push origin main "${{ steps.update.outputs.version }}"
          fi
          
